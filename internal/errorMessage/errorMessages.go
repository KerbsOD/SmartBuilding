package errorMessage

// VariableCapacity
const InvalidCapacitiesErrorMessage = "initial capacity must be smaller than regular capacity"
const InvalidRainingPrice = "raining price must be higher than regular price"
